import { SignUp } from '../../../src/domain/use-cases/sign-up';

describe('sign-up', () => {
  it('return User when signup successful', () => {
    //arrange

    const sut = new SignUp();

    //act

    //assert
  });
});
